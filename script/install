#!/bin/bash

set -o errexit
set -o pipefail

readonly SRC_DIR=auto_qc

./script/bootstrap
if [ $? -ne 0 ]
    then
    # Exit here if bootstrap not successful
    exit 1
fi

cp -r env bin $SRC_DIR VERSION $PREFIX
